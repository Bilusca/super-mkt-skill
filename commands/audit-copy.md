---
description: Audit existing copy — a page, a document, a URL, an ad or an email — and return scores plus prioritized fixes
argument-hint: [url | file path | pasted copy]
---

Run the super-mkt skill in **audit mode** on: $ARGUMENTS

Read `skills/super-mkt/SKILL.md` and `skills/super-mkt/references/audit.md` from the plugin, then follow the audit procedure exactly:

1. Get the source for real — fetch the URL, read the file, or use the pasted text. If you cannot reach it, ask for the content instead of guessing.
2. Ask the three context questions (goal, audience and awareness stage, current numbers), unless the user already answered them or told you to just audit it.
3. Score the ten dimensions 0–10.
4. Report in the documented format: verdict, scorecard, what's working, findings ordered by severity with quoted excerpts and rewrites, AI-slop flags, priority plan, a rewrite of the weakest critical block, and what to test next.

Do not inflate scores. Every finding ships with its fix.
